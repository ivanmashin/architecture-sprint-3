apiVersion: v1
kind: Service
metadata:
  name: {{ include "devices.fullname" . }}
  labels:
    {{- include "devices.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devices.selectorLabels" . | nindent 4 }}
