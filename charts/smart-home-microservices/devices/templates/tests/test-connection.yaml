apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devices.fullname" . }}-test-connection"
  labels:
    {{- include "devices.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devices.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
